## Очистка шапки на desktop

Убрать центральную навигацию (Tours, Guides, Book, Become a guide) из `SiteHeader.tsx` — эти разделы уже доступны в bottom menu bar и в боковом меню (Sheet), которое открывается по аватару.

### Изменения

**`src/components/SiteHeader.tsx`**
- Удалить блок `<nav className="hidden md:flex ...">` (строки 80–91) с центральными ссылками
- Удалить неиспользуемую переменную `navLinks` (строка 69)
- `menuLinks` оставить — он используется в Sheet (боковое меню)

### Результат
Desktop шапка: логотип слева, справа — wishlist, переключатель языка и pill с аватаром/меню. Чисто, без дублирующихся ссылок.