Сделать секцию "Browse by interest" на главной странице горизонтальной полосой вместо сетки.

Что меняется:
- Вместо `grid grid-cols-2 sm:grid-cols-4` — flex-ряд с `overflow-x-auto` и скрытой полосой прокрутки
- Карточки категорий становятся компактными "чипами": иконка + название в одну строку, без описания и без рамки/тени на уровне карточки
- Сохраняется клик — ведёт на `/guides?category=<slug>`
- Добавляется плавный горизонтальный скролл (scroll-smooth, hide scrollbar через стандартные классы)
- На десктопе — ряд из 8-10 категорий, на мобильном — swipe вправо/влево
- Заголовок и подзаголовок секции остаются без изменений
- Строки локализации `browse.title` и `browse.subtitle` не трогаем

Файл для изменения:
- `src/routes/index.tsx` — переписать разметку категорий внутри секции "Browse by interest"

Никаких новых файлов, зависимостей или строк перевода не требуется.